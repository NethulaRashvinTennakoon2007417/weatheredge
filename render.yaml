services:
  - type: web
    name: weatheredge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: WEATHER_SECRET_KEY
        sync: false   # You set this manually in Render dashboard — never commit it
    autoDeploy: true
